{
  "buildCommand": "bundle exec jekyll build --verbose",
  "outputDirectory": "_site",
  "installCommand": "gem install bundler && bundle install",
  "framework": null,
  "nodeVersion": "18.x",
  "env": {
    "BUNDLE_WITHOUT": "development:test"
  }
}